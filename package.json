{
  "name": "sokoforge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["web"],
  "scripts": {
    "dev": "npm --workspace web run dev",
    "build": "npm run wasm:build && npm --workspace web run build",
    "wasm:build": "wasm-pack build crates/sokoforge-wasm --target web --out-dir ../../web/public/wasm --out-name sokoforge_wasm --release",
    "test": "cargo test --workspace && npm --workspace web run test",
    "lint": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && npm --workspace web run typecheck",
    "cli": "cargo run -p sokoforge-cli --"
  },
  "devDependencies": {
    "wasm-pack": "^0.15.0"
  }
}
